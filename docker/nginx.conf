server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: las rutas de React Router se resuelven en el cliente
    location / {
        try_files $uri $uri/ /index.html;
    }
}
